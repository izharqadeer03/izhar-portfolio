import { SystemShell } from '@/components/system/SystemShell';

/**
 * `/` is the operating system. There is no hero section, no marketing copy and
 * no scroll — the desktop itself is the landing experience.
 */
export default function Home() {
  return <SystemShell />;
}
